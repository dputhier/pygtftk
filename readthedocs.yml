conda:
   file: conda/env_read_the_doc.yaml

build:
   image: latest
    
requirements_file: requirements_developers.txt

python:
   version: 3.6
   setup_py_install: true
   pip_install: true
   extra_requirements:
      - tests
      - docs
